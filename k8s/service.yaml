apiVersion: v1
kind: Service
metadata:
  name: devops-learning-app-service
spec:
  selector:
    app: devops-learning-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort
